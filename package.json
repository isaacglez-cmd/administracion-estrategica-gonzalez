{
  "name": "administracion-estrategica-gonzalez",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "check:syntax": "for file in *.js; do node --check \"$file\"; done",
    "test": "npm run check:syntax && node scripts/qa-static.mjs"
  }
}
