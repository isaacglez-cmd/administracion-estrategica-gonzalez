(()=>{const KEY='ae-evidence-2026';let s;try{s=JSON.parse(localStorage.getItem(KEY))||{}}catch(e){s={}};s.conf=s.conf||{};const save=()=>localStorage.setItem(KEY,JSON.stringify(s));const chapters=[...document.querySelectorAll('section[id],article[id]')].filter(x=>/^chap\d+$/i.test(x.id)).slice(0,10);const getAnswers=ch=>[...ch.querySelectorAll('textarea')].filter(x=>x.value.trim().length>=20).length;const getChecks=ch=>[...ch.querySelectorAll('input[type="checkbox"]')].filter(x=>x.checked).length;const getQuiz=ch=>[...ch.querySelectorAll('.option.correct')].length;const evidence=ch=>{const a=getAnswers(ch),c=getChecks(ch),q=getQuiz(ch);return{a,c,q,score:Math.min(100,(a?35:0)+(c?25:0)+(q?40:0))}};function inject(){chapters.forEach((ch,i)=>{if(ch.querySelector('.ae-evidence'))return;const box=document.createElement('div');box.className='ae-evidence';box.innerHTML=`<h3>Evidencia de aprendizaje</h3><div class="ae-master"><i></i></div><div class="ae-evidence-grid"><div class="ae-evidence-card"><b data-a>0</b><span>RESPUESTAS DESARROLLADAS</span></div><div class="ae-evidence-card"><b data-c>0</b><span>ACTIVIDADES MARCADAS</span></div><div class="ae-evidence-card"><b data-q>0</b><span>ACIERTOS REGISTRADOS</span></div></div><div class="ae-selfcheck" role="group" aria-label="Autoevaluación del capítulo"><button type="button" data-v="1">Necesito repasar</button><button type="button" data-v="2">Puedo explicarlo con apoyo</button><button type="button" data-v="3">Puedo aplicarlo a un caso nuevo</button></div><div class="ae-evidence-note">El porcentaje combina evidencia observable en este dispositivo. No sustituye la evaluación docente.</div>`;ch.appendChild(box);const update=()=>{const e=evidence(ch);box.querySelector('[data-a]').textContent=e.a;box.querySelector('[data-c]').textContent=e.c;box.querySelector('[data-q]').textContent=e.q;box.querySelector('.ae-master i').style.width=e.score+'%';box.querySelectorAll('[data-v]').forEach(b=>b.setAttribute('aria-pressed',String(Number(b.dataset.v)===s.conf[i])))};box.querySelectorAll('[data-v]').forEach(b=>b.onclick=()=>{s.conf[i]=Number(b.dataset.v);save();update()});ch.addEventListener('input',update);ch.addEventListener('change',update);ch.addEventListener('click',()=>setTimeout(update,50));update()})}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',inject);else inject();})();